
  # VYSPER INSTITUTE Web App

  This is a branded web app for VYSPER INSTITUTE.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
